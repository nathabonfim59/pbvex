[@pbvex/protocol](../index.md) / formatOpaqueId

# Function: formatOpaqueId()

> **formatOpaqueId**(`envelope`, `mac`): `string`

Formats a structural ID envelope with an already-authenticated MAC.

## Parameters

### envelope

`Omit`\<[`OpaqueIdEnvelope`](../interfaces/OpaqueIdEnvelope.md), `"mac"`\>

### mac

`Uint8Array`

## Returns

`string`
